import PostLay from "../Layout/PostLay";

const Post = ({ isPhone }) => {
  return (
    <div className="w-screen h-screen flex justify-center items-center">
      <PostLay />
    </div>
  );
};
export default Post;
